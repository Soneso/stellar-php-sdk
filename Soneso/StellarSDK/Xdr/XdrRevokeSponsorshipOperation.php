<?php declare(strict_types=1);

// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

namespace Soneso\StellarSDK\Xdr;

class XdrRevokeSponsorshipOperation {

    public XdrRevokeSponsorshipType $type;
    public ?XdrLedgerKey $ledgerKey = null;
    public ?XdrRevokeSponsorshipSigner $signer = null;

    public function __construct(?XdrRevokeSponsorshipType $type = null) {
        if ($type !== null) {
            $this->type = $type;
        }
    }

    public function encode(): string {
        $bytes = $this->type->encode();
        switch ($this->type->getValue()) {
            case XdrRevokeSponsorshipType::LEDGER_ENTRY:
                $bytes .= $this->ledgerKey->encode();
                break;
            case XdrRevokeSponsorshipType::SIGNER:
                $bytes .= $this->signer->encode();
                break;
            default:
                break;
        }
        return $bytes;
    }

    public static function decode(XdrBuffer $xdr): XdrRevokeSponsorshipOperation {
        $result = new XdrRevokeSponsorshipOperation(XdrRevokeSponsorshipType::decode($xdr));
        switch ($result->type->getValue()) {
            case XdrRevokeSponsorshipType::LEDGER_ENTRY:
                $result->ledgerKey = XdrLedgerKey::decode($xdr);
                break;
            case XdrRevokeSponsorshipType::SIGNER:
                $result->signer = XdrRevokeSponsorshipSigner::decode($xdr);
                break;
            default:
                break;
        }
        return $result;
    }

    public function getType(): XdrRevokeSponsorshipType { return $this->type; }
    public function setType(XdrRevokeSponsorshipType $type): void { $this->type = $type; }
    public function getLedgerKey(): ?XdrLedgerKey { return $this->ledgerKey; }
    public function setLedgerKey(?XdrLedgerKey $ledgerKey): void { $this->ledgerKey = $ledgerKey; }
    public function getSigner(): ?XdrRevokeSponsorshipSigner { return $this->signer; }
    public function setSigner(?XdrRevokeSponsorshipSigner $signer): void { $this->signer = $signer; }

    public function toBase64Xdr(): string {
        return base64_encode($this->encode());
    }

    public static function fromBase64Xdr(string $xdr): static {
        $decoded = base64_decode($xdr, true);
        if ($decoded === false) {
            throw new \InvalidArgumentException('Invalid base64-encoded XDR');
        }
        return static::decode(new XdrBuffer($decoded));
    }

    public function toTxRep(string $prefix, array &$lines): void {
        $this->type->toTxRep($prefix . '.type', $lines);
        switch ($this->type->getValue()) {
            case XdrRevokeSponsorshipType::LEDGER_ENTRY:
                $this->ledgerKey->toTxRep($prefix . '.ledgerKey', $lines);
                break;
            case XdrRevokeSponsorshipType::SIGNER:
                $this->signer->toTxRep($prefix . '.signer', $lines);
                break;
            default:
                break;
        }
    }

    public static function fromTxRep(array $map, string $prefix): XdrRevokeSponsorshipOperation {
        $disc = XdrRevokeSponsorshipType::fromTxRep($map, $prefix . '.type');
        $result = new XdrRevokeSponsorshipOperation($disc);
        switch ($result->type->getValue()) {
            case XdrRevokeSponsorshipType::LEDGER_ENTRY:
                $result->ledgerKey = XdrLedgerKey::fromTxRep($map, $prefix . '.ledgerKey');
                break;
            case XdrRevokeSponsorshipType::SIGNER:
                $result->signer = XdrRevokeSponsorshipSigner::fromTxRep($map, $prefix . '.signer');
                break;
            default:
                break;
        }
        return $result;
    }
}
