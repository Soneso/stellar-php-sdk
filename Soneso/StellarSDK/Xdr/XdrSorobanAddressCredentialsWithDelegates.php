<?php declare(strict_types=1);

// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

namespace Soneso\StellarSDK\Xdr;

use InvalidArgumentException;
use JsonException;

class XdrSorobanAddressCredentialsWithDelegates {

    public XdrSorobanAddressCredentials $addressCredentials;
    public array $delegates;

    public function __construct(XdrSorobanAddressCredentials $addressCredentials, array $delegates) {
        $this->addressCredentials = $addressCredentials;
        $this->delegates = $delegates;
    }

    public function encode(): string {
        $bytes = $this->addressCredentials->encode();
        $delegatesCount = count($this->delegates);
        $bytes .= XdrEncoder::integer32($delegatesCount);
        foreach ($this->delegates as $delegatesItem) {
            $bytes .= $delegatesItem->encode();
        }
        return $bytes;
    }

    public static function decode(XdrBuffer $xdr): XdrSorobanAddressCredentialsWithDelegates {
        $addressCredentials = XdrSorobanAddressCredentials::decode($xdr);
        $delegates = [];
        $delegatesSize = $xdr->readInteger32();
        for ($i = 0; $i < $delegatesSize; $i++) {
            $delegates[] = XdrSorobanDelegateSignature::decode($xdr);
        }
        return new XdrSorobanAddressCredentialsWithDelegates($addressCredentials, $delegates);
    }

    public function getAddressCredentials(): XdrSorobanAddressCredentials { return $this->addressCredentials; }
    public function setAddressCredentials(XdrSorobanAddressCredentials $addressCredentials): void { $this->addressCredentials = $addressCredentials; }
    public function getDelegates(): array { return $this->delegates; }
    public function setDelegates(array $delegates): void { $this->delegates = $delegates; }

    public function toBase64Xdr(): string {
        return base64_encode($this->encode());
    }

    public static function fromBase64Xdr(string $xdr): static {
        $decoded = base64_decode($xdr, true);
        if ($decoded === false) {
            throw new InvalidArgumentException('Invalid base64-encoded XDR');
        }
        return static::decode(new XdrBuffer($decoded));
    }

    public function toJsonValue(): array {
        return [
            'address_credentials' => $this->addressCredentials->toJsonValue(),
            'delegates' => array_map(static function ($item) { return $item->toJsonValue(); }, $this->delegates),
        ];
    }

    public static function fromJsonValue(mixed $value): static {
        if (is_array($value) && array_key_exists('$schema', $value)) {
            unset($value['$schema']);
        }
        if (!is_array($value)) {
            throw new InvalidArgumentException(
                'Expected object for XdrSorobanAddressCredentialsWithDelegates JSON value, got ' . get_debug_type($value)
            );
        }
        if (!array_key_exists('address_credentials', $value)) {
            throw new InvalidArgumentException(
                'Missing required field address_credentials for XdrSorobanAddressCredentialsWithDelegates'
            );
        }
        $addressCredentials = XdrSorobanAddressCredentials::fromJsonValue($value['address_credentials']);
        if (!array_key_exists('delegates', $value)) {
            throw new InvalidArgumentException(
                'Missing required field delegates for XdrSorobanAddressCredentialsWithDelegates'
            );
        }
        $delegates = (static function ($v) {
            if (!is_array($v)) {
                throw new InvalidArgumentException('Expected JSON array, got ' . get_debug_type($v));
            }
            $out = [];
            foreach ($v as $item) { $out[] = XdrSorobanDelegateSignature::fromJsonValue($item); }
            return $out;
        })($value['delegates']);
        return new static($addressCredentials, $delegates);
    }

    /**
     * @throws JsonException If the value contains structures that cannot be encoded as JSON.
     */
    public function toJson(): string {
        return json_encode(
            $this->toJsonValue(),
            JSON_THROW_ON_ERROR | JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE
        );
    }

    /**
     * @throws JsonException If $json is not syntactically valid JSON.
     * @throws InvalidArgumentException If the JSON shape does not match this type.
     */
    public static function fromJson(string $json): static {
        return static::fromJsonValue(json_decode($json, true, 512, JSON_THROW_ON_ERROR));
    }

    public function toTxRep(string $prefix, array &$lines): void {
        $this->addressCredentials->toTxRep($prefix . '.addressCredentials', $lines);
        $lines[$prefix . '.delegates.len'] = (string)count($this->delegates);
        for ($i = 0; $i < count($this->delegates); $i++) {
            $this->delegates[$i]->toTxRep($prefix . '.delegates[' . $i . ']', $lines);
        }
    }

    public static function fromTxRep(array $map, string $prefix): XdrSorobanAddressCredentialsWithDelegates {
        $addressCredentials = XdrSorobanAddressCredentials::fromTxRep($map, $prefix . '.addressCredentials');
        $delegatesLen = TxRepHelper::parseInt(TxRepHelper::getValue($map, $prefix . '.delegates.len') ?? '0');
        $delegates = [];
        for ($i = 0; $i < $delegatesLen; $i++) {
            $delegates[] = XdrSorobanDelegateSignature::fromTxRep($map, $prefix . '.delegates[' . $i . ']');
        }
        return new XdrSorobanAddressCredentialsWithDelegates($addressCredentials, $delegates);
    }
}
