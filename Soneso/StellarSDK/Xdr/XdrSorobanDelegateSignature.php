<?php declare(strict_types=1);

// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

namespace Soneso\StellarSDK\Xdr;

use InvalidArgumentException;
use JsonException;

class XdrSorobanDelegateSignature {

    public XdrSCAddress $address;
    public XdrSCVal $signature;
    public array $nestedDelegates;

    public function __construct(XdrSCAddress $address, XdrSCVal $signature, array $nestedDelegates) {
        $this->address = $address;
        $this->signature = $signature;
        $this->nestedDelegates = $nestedDelegates;
    }

    public function encode(): string {
        $bytes = $this->address->encode();
        $bytes .= $this->signature->encode();
        $nestedDelegatesCount = count($this->nestedDelegates);
        $bytes .= XdrEncoder::integer32($nestedDelegatesCount);
        foreach ($this->nestedDelegates as $nestedDelegatesItem) {
            $bytes .= $nestedDelegatesItem->encode();
        }
        return $bytes;
    }

    public static function decode(XdrBuffer $xdr): XdrSorobanDelegateSignature {
        $xdr->enterRecursion();
        try {
            $address = XdrSCAddress::decode($xdr);
            $signature = XdrSCVal::decode($xdr);
            $nestedDelegates = [];
            $nestedDelegatesSize = $xdr->readInteger32();
            for ($i = 0; $i < $nestedDelegatesSize; $i++) {
                $nestedDelegates[] = XdrSorobanDelegateSignature::decode($xdr);
            }
            return new XdrSorobanDelegateSignature($address, $signature, $nestedDelegates);
        } finally {
            $xdr->leaveRecursion();
        }
    }

    public function getAddress(): XdrSCAddress { return $this->address; }
    public function setAddress(XdrSCAddress $address): void { $this->address = $address; }
    public function getSignature(): XdrSCVal { return $this->signature; }
    public function setSignature(XdrSCVal $signature): void { $this->signature = $signature; }
    public function getNestedDelegates(): array { return $this->nestedDelegates; }
    public function setNestedDelegates(array $nestedDelegates): void { $this->nestedDelegates = $nestedDelegates; }

    public function toBase64Xdr(): string {
        return base64_encode($this->encode());
    }

    public static function fromBase64Xdr(string $xdr): static {
        $decoded = base64_decode($xdr, true);
        if ($decoded === false) {
            throw new InvalidArgumentException('Invalid base64-encoded XDR');
        }
        return static::decode(new XdrBuffer($decoded));
    }

    public function toJsonValue(): array {
        return [
            'address' => $this->address->toJsonValue(),
            'signature' => $this->signature->toJsonValue(),
            'nested_delegates' => array_map(static function ($item) { return $item->toJsonValue(); }, $this->nestedDelegates),
        ];
    }

    public static function fromJsonValue(mixed $value): static {
        if (is_array($value) && array_key_exists('$schema', $value)) {
            unset($value['$schema']);
        }
        if (!is_array($value)) {
            throw new InvalidArgumentException(
                'Expected object for XdrSorobanDelegateSignature JSON value, got ' . get_debug_type($value)
            );
        }
        if (!array_key_exists('address', $value)) {
            throw new InvalidArgumentException(
                'Missing required field address for XdrSorobanDelegateSignature'
            );
        }
        $address = XdrSCAddress::fromJsonValue($value['address']);
        if (!array_key_exists('signature', $value)) {
            throw new InvalidArgumentException(
                'Missing required field signature for XdrSorobanDelegateSignature'
            );
        }
        $signature = XdrSCVal::fromJsonValue($value['signature']);
        if (!array_key_exists('nested_delegates', $value)) {
            throw new InvalidArgumentException(
                'Missing required field nested_delegates for XdrSorobanDelegateSignature'
            );
        }
        $nestedDelegates = (static function ($v) {
            if (!is_array($v)) {
                throw new InvalidArgumentException('Expected JSON array, got ' . get_debug_type($v));
            }
            $out = [];
            foreach ($v as $item) { $out[] = XdrSorobanDelegateSignature::fromJsonValue($item); }
            return $out;
        })($value['nested_delegates']);
        return new static($address, $signature, $nestedDelegates);
    }

    /**
     * @throws JsonException If the value contains structures that cannot be encoded as JSON.
     */
    public function toJson(): string {
        return json_encode(
            $this->toJsonValue(),
            JSON_THROW_ON_ERROR | JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE
        );
    }

    /**
     * @throws JsonException If $json is not syntactically valid JSON.
     * @throws InvalidArgumentException If the JSON shape does not match this type.
     */
    public static function fromJson(string $json): static {
        return static::fromJsonValue(json_decode($json, true, 512, JSON_THROW_ON_ERROR));
    }

    public function toTxRep(string $prefix, array &$lines): void {
        $this->address->toTxRep($prefix . '.address', $lines);
        $this->signature->toTxRep($prefix . '.signature', $lines);
        $lines[$prefix . '.nestedDelegates.len'] = (string)count($this->nestedDelegates);
        for ($i = 0; $i < count($this->nestedDelegates); $i++) {
            $this->nestedDelegates[$i]->toTxRep($prefix . '.nestedDelegates[' . $i . ']', $lines);
        }
    }

    public static function fromTxRep(array $map, string $prefix): XdrSorobanDelegateSignature {
        $address = XdrSCAddress::fromTxRep($map, $prefix . '.address');
        $signature = XdrSCVal::fromTxRep($map, $prefix . '.signature');
        $nestedDelegatesLen = TxRepHelper::parseInt(TxRepHelper::getValue($map, $prefix . '.nestedDelegates.len') ?? '0');
        $nestedDelegates = [];
        for ($i = 0; $i < $nestedDelegatesLen; $i++) {
            $nestedDelegates[] = XdrSorobanDelegateSignature::fromTxRep($map, $prefix . '.nestedDelegates[' . $i . ']');
        }
        return new XdrSorobanDelegateSignature($address, $signature, $nestedDelegates);
    }
}
