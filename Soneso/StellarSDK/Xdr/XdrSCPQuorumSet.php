<?php declare(strict_types=1);

// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

namespace Soneso\StellarSDK\Xdr;

use InvalidArgumentException;
use JsonException;

class XdrSCPQuorumSet {

    public int $threshold;
    public array $validators;
    public array $innerSets;

    public function __construct(int $threshold, array $validators, array $innerSets) {
        $this->threshold = $threshold;
        $this->validators = $validators;
        $this->innerSets = $innerSets;
    }

    public function encode(): string {
        $bytes = XdrEncoder::unsignedInteger32($this->threshold);
        $validatorsCount = count($this->validators);
        $bytes .= XdrEncoder::integer32($validatorsCount);
        foreach ($this->validators as $validatorsItem) {
            $bytes .= $validatorsItem->encode();
        }
        $innerSetsCount = count($this->innerSets);
        $bytes .= XdrEncoder::integer32($innerSetsCount);
        foreach ($this->innerSets as $innerSetsItem) {
            $bytes .= $innerSetsItem->encode();
        }
        return $bytes;
    }

    public static function decode(XdrBuffer $xdr): XdrSCPQuorumSet {
        $threshold = $xdr->readUnsignedInteger32();
        $validators = [];
        $validatorsSize = $xdr->readInteger32();
        for ($i = 0; $i < $validatorsSize; $i++) {
            $validators[] = XdrNodeID::decode($xdr);
        }
        $innerSets = [];
        $innerSetsSize = $xdr->readInteger32();
        for ($i = 0; $i < $innerSetsSize; $i++) {
            $innerSets[] = XdrSCPQuorumSet::decode($xdr);
        }
        return new XdrSCPQuorumSet($threshold, $validators, $innerSets);
    }

    public function getThreshold(): int { return $this->threshold; }
    public function setThreshold(int $threshold): void { $this->threshold = $threshold; }
    public function getValidators(): array { return $this->validators; }
    public function setValidators(array $validators): void { $this->validators = $validators; }
    public function getInnerSets(): array { return $this->innerSets; }
    public function setInnerSets(array $innerSets): void { $this->innerSets = $innerSets; }

    public function toBase64Xdr(): string {
        return base64_encode($this->encode());
    }

    public static function fromBase64Xdr(string $xdr): static {
        $decoded = base64_decode($xdr, true);
        if ($decoded === false) {
            throw new InvalidArgumentException('Invalid base64-encoded XDR');
        }
        return static::decode(new XdrBuffer($decoded));
    }

    public function toJsonValue(): array {
        return [
            'threshold' => $this->threshold,
            'validators' => array_map(static function ($item) { return $item->toJsonValue(); }, $this->validators),
            'inner_sets' => array_map(static function ($item) { return $item->toJsonValue(); }, $this->innerSets),
        ];
    }

    public static function fromJsonValue(mixed $value): static {
        if (is_array($value) && array_key_exists('$schema', $value)) {
            unset($value['$schema']);
        }
        if (!is_array($value)) {
            throw new InvalidArgumentException(
                'Expected object for XdrSCPQuorumSet JSON value, got ' . get_debug_type($value)
            );
        }
        if (!array_key_exists('threshold', $value)) {
            throw new InvalidArgumentException(
                'Missing required field threshold for XdrSCPQuorumSet'
            );
        }
        $threshold = (static function ($v) { if (!is_int($v)) { throw new InvalidArgumentException('Expected int JSON value, got ' . get_debug_type($v)); } return $v; })($value['threshold']);
        if (!array_key_exists('validators', $value)) {
            throw new InvalidArgumentException(
                'Missing required field validators for XdrSCPQuorumSet'
            );
        }
        $validators = (static function ($v) {
            if (!is_array($v)) {
                throw new InvalidArgumentException('Expected JSON array, got ' . get_debug_type($v));
            }
            $out = [];
            foreach ($v as $item) { $out[] = XdrNodeID::fromJsonValue($item); }
            return $out;
        })($value['validators']);
        if (!array_key_exists('inner_sets', $value)) {
            throw new InvalidArgumentException(
                'Missing required field inner_sets for XdrSCPQuorumSet'
            );
        }
        $innerSets = (static function ($v) {
            if (!is_array($v)) {
                throw new InvalidArgumentException('Expected JSON array, got ' . get_debug_type($v));
            }
            $out = [];
            foreach ($v as $item) { $out[] = XdrSCPQuorumSet::fromJsonValue($item); }
            return $out;
        })($value['inner_sets']);
        return new static($threshold, $validators, $innerSets);
    }

    /**
     * @throws JsonException If the value contains structures that cannot be encoded as JSON.
     */
    public function toJson(): string {
        return json_encode(
            $this->toJsonValue(),
            JSON_THROW_ON_ERROR | JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE
        );
    }

    /**
     * @throws JsonException If $json is not syntactically valid JSON.
     * @throws InvalidArgumentException If the JSON shape does not match this type.
     */
    public static function fromJson(string $json): static {
        return static::fromJsonValue(json_decode($json, true, 512, JSON_THROW_ON_ERROR));
    }
}
