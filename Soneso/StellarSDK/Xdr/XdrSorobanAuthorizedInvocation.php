<?php declare(strict_types=1);

// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

namespace Soneso\StellarSDK\Xdr;

use InvalidArgumentException;
use JsonException;

class XdrSorobanAuthorizedInvocation {

    public XdrSorobanAuthorizedFunction $function;
    public array $subInvocations;

    public function __construct(XdrSorobanAuthorizedFunction $function, array $subInvocations) {
        $this->function = $function;
        $this->subInvocations = $subInvocations;
    }

    public function encode(): string {
        $bytes = $this->function->encode();
        $subInvocationsCount = count($this->subInvocations);
        $bytes .= XdrEncoder::integer32($subInvocationsCount);
        foreach ($this->subInvocations as $subInvocationsItem) {
            $bytes .= $subInvocationsItem->encode();
        }
        return $bytes;
    }

    public static function decode(XdrBuffer $xdr): XdrSorobanAuthorizedInvocation {
        $function = XdrSorobanAuthorizedFunction::decode($xdr);
        $subInvocations = [];
        $subInvocationsSize = $xdr->readInteger32();
        for ($i = 0; $i < $subInvocationsSize; $i++) {
            $subInvocations[] = XdrSorobanAuthorizedInvocation::decode($xdr);
        }
        return new XdrSorobanAuthorizedInvocation($function, $subInvocations);
    }

    public function getFunction(): XdrSorobanAuthorizedFunction { return $this->function; }
    public function setFunction(XdrSorobanAuthorizedFunction $function): void { $this->function = $function; }
    public function getSubInvocations(): array { return $this->subInvocations; }
    public function setSubInvocations(array $subInvocations): void { $this->subInvocations = $subInvocations; }

    public function toBase64Xdr(): string {
        return base64_encode($this->encode());
    }

    public static function fromBase64Xdr(string $xdr): static {
        $decoded = base64_decode($xdr, true);
        if ($decoded === false) {
            throw new InvalidArgumentException('Invalid base64-encoded XDR');
        }
        return static::decode(new XdrBuffer($decoded));
    }

    public function toJsonValue(): array {
        return [
            'function' => $this->function->toJsonValue(),
            'sub_invocations' => array_map(static function ($item) { return $item->toJsonValue(); }, $this->subInvocations),
        ];
    }

    public static function fromJsonValue(mixed $value): static {
        if (is_array($value) && array_key_exists('$schema', $value)) {
            unset($value['$schema']);
        }
        if (!is_array($value)) {
            throw new InvalidArgumentException(
                'Expected object for XdrSorobanAuthorizedInvocation JSON value, got ' . get_debug_type($value)
            );
        }
        if (!array_key_exists('function', $value)) {
            throw new InvalidArgumentException(
                'Missing required field function for XdrSorobanAuthorizedInvocation'
            );
        }
        $function = XdrSorobanAuthorizedFunction::fromJsonValue($value['function']);
        if (!array_key_exists('sub_invocations', $value)) {
            throw new InvalidArgumentException(
                'Missing required field sub_invocations for XdrSorobanAuthorizedInvocation'
            );
        }
        $subInvocations = (static function ($v) {
            if (!is_array($v)) {
                throw new InvalidArgumentException('Expected JSON array, got ' . get_debug_type($v));
            }
            $out = [];
            foreach ($v as $item) { $out[] = XdrSorobanAuthorizedInvocation::fromJsonValue($item); }
            return $out;
        })($value['sub_invocations']);
        return new static($function, $subInvocations);
    }

    /**
     * @throws JsonException If the value contains structures that cannot be encoded as JSON.
     */
    public function toJson(): string {
        return json_encode(
            $this->toJsonValue(),
            JSON_THROW_ON_ERROR | JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE
        );
    }

    /**
     * @throws JsonException If $json is not syntactically valid JSON.
     * @throws InvalidArgumentException If the JSON shape does not match this type.
     */
    public static function fromJson(string $json): static {
        return static::fromJsonValue(json_decode($json, true, 512, JSON_THROW_ON_ERROR));
    }

    public function toTxRep(string $prefix, array &$lines): void {
        $this->function->toTxRep($prefix . '.function', $lines);
        $lines[$prefix . '.subInvocations.len'] = (string)count($this->subInvocations);
        for ($i = 0; $i < count($this->subInvocations); $i++) {
            $this->subInvocations[$i]->toTxRep($prefix . '.subInvocations[' . $i . ']', $lines);
        }
    }

    public static function fromTxRep(array $map, string $prefix): XdrSorobanAuthorizedInvocation {
        $function = XdrSorobanAuthorizedFunction::fromTxRep($map, $prefix . '.function');
        $subInvocationsLen = TxRepHelper::parseInt(TxRepHelper::getValue($map, $prefix . '.subInvocations.len') ?? '0');
        $subInvocations = [];
        for ($i = 0; $i < $subInvocationsLen; $i++) {
            $subInvocations[] = XdrSorobanAuthorizedInvocation::fromTxRep($map, $prefix . '.subInvocations[' . $i . ']');
        }
        return new XdrSorobanAuthorizedInvocation($function, $subInvocations);
    }
}
