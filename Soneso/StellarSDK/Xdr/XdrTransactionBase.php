<?php declare(strict_types=1);

// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

namespace Soneso\StellarSDK\Xdr;

use InvalidArgumentException;
use JsonException;

class XdrTransactionBase {

    public XdrMuxedAccount $sourceAccount;
    public int $fee;
    public XdrSequenceNumber $sequenceNumber;
    public XdrPreconditions $preconditions;
    public XdrMemo $memo;
    public array $operations;
    public XdrTransactionExt $ext;

    public function __construct(XdrMuxedAccount $sourceAccount, int $fee, XdrSequenceNumber $sequenceNumber, XdrPreconditions $preconditions, XdrMemo $memo, array $operations, XdrTransactionExt $ext) {
        $this->sourceAccount = $sourceAccount;
        $this->fee = $fee;
        $this->sequenceNumber = $sequenceNumber;
        $this->preconditions = $preconditions;
        $this->memo = $memo;
        $this->operations = $operations;
        $this->ext = $ext;
    }

    public function encode(): string {
        $bytes = $this->sourceAccount->encode();
        $bytes .= XdrEncoder::unsignedInteger32($this->fee);
        $bytes .= $this->sequenceNumber->encode();
        $bytes .= $this->preconditions->encode();
        $bytes .= $this->memo->encode();
        $operationsCount = count($this->operations);
        $bytes .= XdrEncoder::integer32($operationsCount);
        foreach ($this->operations as $operationsItem) {
            $bytes .= $operationsItem->encode();
        }
        $bytes .= $this->ext->encode();
        return $bytes;
    }

    public static function decode(XdrBuffer $xdr): static {
        $sourceAccount = XdrMuxedAccount::decode($xdr);
        $fee = $xdr->readUnsignedInteger32();
        $sequenceNumber = XdrSequenceNumber::decode($xdr);
        $preconditions = XdrPreconditions::decode($xdr);
        $memo = XdrMemo::decode($xdr);
        $operations = [];
        $operationsSize = $xdr->readInteger32();
        for ($i = 0; $i < $operationsSize; $i++) {
            $operations[] = XdrOperation::decode($xdr);
        }
        $ext = XdrTransactionExt::decode($xdr);
        return new static($sourceAccount, $fee, $sequenceNumber, $preconditions, $memo, $operations, $ext);
    }

    public function getSourceAccount(): XdrMuxedAccount { return $this->sourceAccount; }
    public function setSourceAccount(XdrMuxedAccount $sourceAccount): void { $this->sourceAccount = $sourceAccount; }
    public function getFee(): int { return $this->fee; }
    public function setFee(int $fee): void { $this->fee = $fee; }
    public function getSequenceNumber(): XdrSequenceNumber { return $this->sequenceNumber; }
    public function setSequenceNumber(XdrSequenceNumber $sequenceNumber): void { $this->sequenceNumber = $sequenceNumber; }
    public function getPreconditions(): XdrPreconditions { return $this->preconditions; }
    public function setPreconditions(XdrPreconditions $preconditions): void { $this->preconditions = $preconditions; }
    public function getMemo(): XdrMemo { return $this->memo; }
    public function setMemo(XdrMemo $memo): void { $this->memo = $memo; }
    public function getOperations(): array { return $this->operations; }
    public function setOperations(array $operations): void { $this->operations = $operations; }
    public function getExt(): XdrTransactionExt { return $this->ext; }
    public function setExt(XdrTransactionExt $ext): void { $this->ext = $ext; }

    public function toBase64Xdr(): string {
        return base64_encode($this->encode());
    }

    public static function fromBase64Xdr(string $xdr): static {
        $decoded = base64_decode($xdr, true);
        if ($decoded === false) {
            throw new InvalidArgumentException('Invalid base64-encoded XDR');
        }
        return static::decode(new XdrBuffer($decoded));
    }

    public function toJsonValue(): array {
        return [
            'source_account' => $this->sourceAccount->toJsonValue(),
            'fee' => $this->fee,
            'seq_num' => $this->sequenceNumber->toJsonValue(),
            'cond' => $this->preconditions->toJsonValue(),
            'memo' => $this->memo->toJsonValue(),
            'operations' => array_map(static function ($item) { return $item->toJsonValue(); }, $this->operations),
            'ext' => $this->ext->toJsonValue(),
        ];
    }

    public static function fromJsonValue(mixed $value): static {
        if (is_array($value) && array_key_exists('$schema', $value)) {
            unset($value['$schema']);
        }
        if (!is_array($value)) {
            throw new InvalidArgumentException(
                'Expected object for XdrTransaction JSON value, got ' . get_debug_type($value)
            );
        }
        foreach (['source_account', 'fee', 'seq_num', 'cond', 'memo', 'operations', 'ext'] as $required) {
            if (!array_key_exists($required, $value)) {
                throw new InvalidArgumentException(
                    'Missing required field ' . $required . ' for XdrTransaction'
                );
            }
        }
        $sourceAccount = XdrMuxedAccount::fromJsonValue($value['source_account']);
        if (!is_int($value['fee'])) {
            throw new InvalidArgumentException(
                    'Expected int for fee, got ' . get_debug_type($value['fee'])
            );
        }
        $fee = $value['fee'];
        $sequenceNumber = XdrSequenceNumber::fromJsonValue($value['seq_num']);
        $preconditions = XdrPreconditions::fromJsonValue($value['cond']);
        $memo = XdrMemo::fromJsonValue($value['memo']);
        if (!is_array($value['operations'])) {
            throw new InvalidArgumentException(
                'Expected JSON array for operations, got ' . get_debug_type($value['operations'])
            );
        }
        $operations = [];
        foreach ($value['operations'] as $item) {
            $operations[] = XdrOperation::fromJsonValue($item);
        }
        $ext = XdrTransactionExt::fromJsonValue($value['ext']);
        // Wrapper signature: (sourceAccount, sequenceNumber, operations, ?fee, ?memo, ?preconditions, ?ext).
        return new static($sourceAccount, $sequenceNumber, $operations, $fee, $memo, $preconditions, $ext);
    }

    /**
     * @throws JsonException If the value contains structures that cannot be encoded as JSON.
     */
    public function toJson(): string {
        return json_encode(
            $this->toJsonValue(),
            JSON_THROW_ON_ERROR | JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE
        );
    }

    /**
     * @throws JsonException If $json is not syntactically valid JSON.
     * @throws InvalidArgumentException If the JSON shape does not match this type.
     */
    public static function fromJson(string $json): static {
        return static::fromJsonValue(json_decode($json, true, 512, JSON_THROW_ON_ERROR));
    }

    public function toTxRep(string $prefix, array &$lines): void {
        $lines[$prefix . '.sourceAccount'] = TxRepHelper::formatMuxedAccount($this->sourceAccount);
        $lines[$prefix . '.fee'] = (string)$this->fee;
        $this->sequenceNumber->toTxRep($prefix . '.seqNum', $lines);
        $this->preconditions->toTxRep($prefix . '.cond', $lines);
        $this->memo->toTxRep($prefix . '.memo', $lines);
        $lines[$prefix . '.operations.len'] = (string)count($this->operations);
        for ($i = 0; $i < count($this->operations); $i++) {
            $this->operations[$i]->toTxRep($prefix . '.operations[' . $i . ']', $lines);
        }
        $this->ext->toTxRep($prefix . '.ext', $lines);
    }

    public static function fromTxRep(array $map, string $prefix): static {
        $sourceAccount = TxRepHelper::parseMuxedAccount(TxRepHelper::getValue($map, $prefix . '.sourceAccount') ?? '');
        $fee = TxRepHelper::parseInt(TxRepHelper::getValue($map, $prefix . '.fee') ?? '0');
        $sequenceNumber = XdrSequenceNumber::fromTxRep($map, $prefix . '.seqNum');
        $preconditions = XdrPreconditions::fromTxRep($map, $prefix . '.cond');
        $memo = XdrMemo::fromTxRep($map, $prefix . '.memo');
        $operationsLen = TxRepHelper::parseInt(TxRepHelper::getValue($map, $prefix . '.operations.len') ?? '0');
        $operations = [];
        for ($i = 0; $i < $operationsLen; $i++) {
            $operations[] = XdrOperation::fromTxRep($map, $prefix . '.operations[' . $i . ']');
        }
        $ext = XdrTransactionExt::fromTxRep($map, $prefix . '.ext');
        return new static($sourceAccount, $fee, $sequenceNumber, $preconditions, $memo, $operations, $ext);
    }
}
