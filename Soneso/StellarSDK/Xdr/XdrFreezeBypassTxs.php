<?php declare(strict_types=1);

// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

namespace Soneso\StellarSDK\Xdr;

use InvalidArgumentException;
use JsonException;

class XdrFreezeBypassTxs {

    public array $txHashes;

    public function __construct(array $txHashes) {
        $this->txHashes = $txHashes;
    }

    public function encode(): string {
        $txHashesCount = count($this->txHashes);
        $bytes = XdrEncoder::integer32($txHashesCount);
        foreach ($this->txHashes as $txHashesItem) {
            $bytes .= XdrEncoder::opaqueFixed($txHashesItem, 32);
        }
        return $bytes;
    }

    public static function decode(XdrBuffer $xdr): XdrFreezeBypassTxs {
        $txHashes = [];
        $txHashesSize = $xdr->readInteger32();
        for ($i = 0; $i < $txHashesSize; $i++) {
            $txHashes[] = $xdr->readOpaqueFixed(32);
        }
        return new XdrFreezeBypassTxs($txHashes);
    }

    public function getTxHashes(): array { return $this->txHashes; }
    public function setTxHashes(array $txHashes): void { $this->txHashes = $txHashes; }

    public function toBase64Xdr(): string {
        return base64_encode($this->encode());
    }

    public static function fromBase64Xdr(string $xdr): static {
        $decoded = base64_decode($xdr, true);
        if ($decoded === false) {
            throw new InvalidArgumentException('Invalid base64-encoded XDR');
        }
        return static::decode(new XdrBuffer($decoded));
    }

    public function toJsonValue(): array {
        return [
            'tx_hashes' => array_map(static function ($item) { return XdrJsonHelper::bytesToHex($item); }, $this->txHashes),
        ];
    }

    public static function fromJsonValue(mixed $value): static {
        if (is_array($value) && array_key_exists('$schema', $value)) {
            unset($value['$schema']);
        }
        if (!is_array($value)) {
            throw new InvalidArgumentException(
                'Expected object for XdrFreezeBypassTxs JSON value, got ' . get_debug_type($value)
            );
        }
        if (!array_key_exists('tx_hashes', $value)) {
            throw new InvalidArgumentException(
                'Missing required field tx_hashes for XdrFreezeBypassTxs'
            );
        }
        $txHashes = (static function ($v) {
            if (!is_array($v)) {
                throw new InvalidArgumentException('Expected JSON array, got ' . get_debug_type($v));
            }
            $out = [];
            foreach ($v as $item) { $out[] = (static function ($v) { if (!is_string($v)) { throw new InvalidArgumentException('Expected hex string JSON value, got ' . get_debug_type($v)); } return XdrJsonHelper::hexToBytes($v); })($item); }
            return $out;
        })($value['tx_hashes']);
        return new static($txHashes);
    }

    /**
     * @throws JsonException If the value contains structures that cannot be encoded as JSON.
     */
    public function toJson(): string {
        return json_encode(
            $this->toJsonValue(),
            JSON_THROW_ON_ERROR | JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE
        );
    }

    /**
     * @throws JsonException If $json is not syntactically valid JSON.
     * @throws InvalidArgumentException If the JSON shape does not match this type.
     */
    public static function fromJson(string $json): static {
        return static::fromJsonValue(json_decode($json, true, 512, JSON_THROW_ON_ERROR));
    }
}
