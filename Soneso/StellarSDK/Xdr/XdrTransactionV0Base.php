<?php declare(strict_types=1);

// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

namespace Soneso\StellarSDK\Xdr;

class XdrTransactionV0Base {

    public string $sourceAccountEd25519;
    public int $fee;
    public XdrSequenceNumber $seqNum;
    public ?XdrTimeBounds $timeBounds = null;
    public XdrMemo $memo;
    public array $operations;
    public XdrTransactionV0Ext $ext;

    public function __construct(string $sourceAccountEd25519, int $fee, XdrSequenceNumber $seqNum, XdrMemo $memo, array $operations, XdrTransactionV0Ext $ext, ?XdrTimeBounds $timeBounds = null) {
        $this->sourceAccountEd25519 = $sourceAccountEd25519;
        $this->fee = $fee;
        $this->seqNum = $seqNum;
        $this->memo = $memo;
        $this->operations = $operations;
        $this->ext = $ext;
        $this->timeBounds = $timeBounds;
    }

    public function encode(): string {
        $bytes = XdrEncoder::opaqueFixed($this->sourceAccountEd25519, 32);
        $bytes .= XdrEncoder::unsignedInteger32($this->fee);
        $bytes .= $this->seqNum->encode();
        if ($this->timeBounds !== null) {
            $bytes .= XdrEncoder::integer32(1);
            $bytes .= $this->timeBounds->encode();
        } else {
            $bytes .= XdrEncoder::integer32(0);
        }
        $bytes .= $this->memo->encode();
        $operationsCount = count($this->operations);
        $bytes .= XdrEncoder::integer32($operationsCount);
        foreach ($this->operations as $operationsItem) {
            $bytes .= $operationsItem->encode();
        }
        $bytes .= $this->ext->encode();
        return $bytes;
    }

    public static function decode(XdrBuffer $xdr): static {
        $sourceAccountEd25519 = $xdr->readOpaqueFixed(32);
        $fee = $xdr->readUnsignedInteger32();
        $seqNum = XdrSequenceNumber::decode($xdr);
        $timeBounds = null;
        if ($xdr->readInteger32() !== 0) {
            $timeBounds = XdrTimeBounds::decode($xdr);
        }
        $memo = XdrMemo::decode($xdr);
        $operations = [];
        $operationsSize = $xdr->readInteger32();
        for ($i = 0; $i < $operationsSize; $i++) {
            $operations[] = XdrOperation::decode($xdr);
        }
        $ext = XdrTransactionV0Ext::decode($xdr);
        return new static($sourceAccountEd25519, $fee, $seqNum, $memo, $operations, $ext, $timeBounds);
    }

    public function getSourceAccountEd25519(): string { return $this->sourceAccountEd25519; }
    public function setSourceAccountEd25519(string $sourceAccountEd25519): void { $this->sourceAccountEd25519 = $sourceAccountEd25519; }
    public function getFee(): int { return $this->fee; }
    public function setFee(int $fee): void { $this->fee = $fee; }
    public function getSeqNum(): XdrSequenceNumber { return $this->seqNum; }
    public function setSeqNum(XdrSequenceNumber $seqNum): void { $this->seqNum = $seqNum; }
    public function getTimeBounds(): ?XdrTimeBounds { return $this->timeBounds; }
    public function setTimeBounds(?XdrTimeBounds $timeBounds): void { $this->timeBounds = $timeBounds; }
    public function getMemo(): XdrMemo { return $this->memo; }
    public function setMemo(XdrMemo $memo): void { $this->memo = $memo; }
    public function getOperations(): array { return $this->operations; }
    public function setOperations(array $operations): void { $this->operations = $operations; }
    public function getExt(): XdrTransactionV0Ext { return $this->ext; }
    public function setExt(XdrTransactionV0Ext $ext): void { $this->ext = $ext; }

    public function toBase64Xdr(): string {
        return base64_encode($this->encode());
    }

    public static function fromBase64Xdr(string $xdr): static {
        $decoded = base64_decode($xdr, true);
        if ($decoded === false) {
            throw new \InvalidArgumentException('Invalid base64-encoded XDR');
        }
        return static::decode(new XdrBuffer($decoded));
    }

    public function toJsonValue(): array {
        return [
            'source_account_ed25519' => XdrJsonHelper::bytesToHex($this->sourceAccountEd25519),
            'fee' => $this->fee,
            'seq_num' => $this->seqNum->toJsonValue(),
            'time_bounds' => ($this->timeBounds !== null ? $this->timeBounds->toJsonValue() : null),
            'memo' => $this->memo->toJsonValue(),
            'operations' => array_map(static function ($item) { return $item->toJsonValue(); }, $this->operations),
            'ext' => $this->ext->toJsonValue(),
        ];
    }

    public static function fromJsonValue(mixed $value): static {
        if (is_array($value) && array_key_exists('$schema', $value)) {
            unset($value['$schema']);
        }
        if (!is_array($value)) {
            throw new \InvalidArgumentException(
                'Expected object for XdrTransactionV0 JSON value, got ' . get_debug_type($value)
            );
        }
        foreach (['source_account_ed25519', 'fee', 'seq_num', 'time_bounds', 'memo', 'operations', 'ext'] as $required) {
            if (!array_key_exists($required, $value)) {
                throw new \InvalidArgumentException(
                    'Missing required field ' . $required . ' for XdrTransactionV0'
                );
            }
        }
        if (!is_string($value['source_account_ed25519'])) {
            throw new \InvalidArgumentException(
                'Expected hex string for source_account_ed25519, got ' . get_debug_type($value['source_account_ed25519'])
            );
        }
        $sourceAccountEd25519 = XdrJsonHelper::hexToBytes($value['source_account_ed25519']);
        if (!is_int($value['fee'])) {
            throw new \InvalidArgumentException(
                    'Expected int for fee, got ' . get_debug_type($value['fee'])
            );
        }
        $fee = $value['fee'];
        $sequenceNumber = XdrSequenceNumber::fromJsonValue($value['seq_num']);
        $timeBounds = null;
        if ($value['time_bounds'] !== null) {
            $timeBounds = XdrTimeBounds::fromJsonValue($value['time_bounds']);
        }
        $memo = XdrMemo::fromJsonValue($value['memo']);
        if (!is_array($value['operations'])) {
            throw new \InvalidArgumentException(
                'Expected JSON array for operations, got ' . get_debug_type($value['operations'])
            );
        }
        $operations = [];
        foreach ($value['operations'] as $item) {
            $operations[] = XdrOperation::fromJsonValue($item);
        }
        $ext = XdrTransactionV0Ext::fromJsonValue($value['ext']);
        // Wrapper signature: (sourceAccountEd25519, sequenceNumber, operations, ?fee, ?memo, ?timeBounds, ?ext).
        return new static($sourceAccountEd25519, $sequenceNumber, $operations, $fee, $memo, $timeBounds, $ext);
    }

    /**
     * @throws \JsonException If the value contains structures that cannot be encoded as JSON.
     */
    public function toJson(): string {
        return json_encode(
            $this->toJsonValue(),
            JSON_THROW_ON_ERROR | JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE
        );
    }

    /**
     * @throws \JsonException If $json is not syntactically valid JSON.
     * @throws \InvalidArgumentException If the JSON shape does not match this type.
     */
    public static function fromJson(string $json): static {
        return static::fromJsonValue(json_decode($json, true, 512, JSON_THROW_ON_ERROR));
    }

    public function toTxRep(string $prefix, array &$lines): void {
        $lines[$prefix . '.sourceAccountEd25519'] = TxRepHelper::bytesToHex($this->sourceAccountEd25519);
        $lines[$prefix . '.fee'] = (string)$this->fee;
        $this->seqNum->toTxRep($prefix . '.seqNum', $lines);
        if ($this->timeBounds !== null) {
            $lines[$prefix . '.timeBounds._present'] = 'true';
            $this->timeBounds->toTxRep($prefix . '.timeBounds', $lines);
        } else {
            $lines[$prefix . '.timeBounds._present'] = 'false';
        }
        $this->memo->toTxRep($prefix . '.memo', $lines);
        $lines[$prefix . '.operations.len'] = (string)count($this->operations);
        for ($i = 0; $i < count($this->operations); $i++) {
            $this->operations[$i]->toTxRep($prefix . '.operations[' . $i . ']', $lines);
        }
        $this->ext->toTxRep($prefix . '.ext', $lines);
    }

    public static function fromTxRep(array $map, string $prefix): static {
        $sourceAccountEd25519 = TxRepHelper::hexToBytes(TxRepHelper::getValue($map, $prefix . '.sourceAccountEd25519') ?? '');
        $fee = TxRepHelper::parseInt(TxRepHelper::getValue($map, $prefix . '.fee') ?? '0');
        $seqNum = XdrSequenceNumber::fromTxRep($map, $prefix . '.seqNum');
        $timeBounds = null;
        $timeBoundsPresent = TxRepHelper::getValue($map, $prefix . '.timeBounds._present');
        if ($timeBoundsPresent !== null && $timeBoundsPresent === 'true') {
            $timeBounds = XdrTimeBounds::fromTxRep($map, $prefix . '.timeBounds');
        }
        $memo = XdrMemo::fromTxRep($map, $prefix . '.memo');
        $operationsLen = TxRepHelper::parseInt(TxRepHelper::getValue($map, $prefix . '.operations.len') ?? '0');
        $operations = [];
        for ($i = 0; $i < $operationsLen; $i++) {
            $operations[] = XdrOperation::fromTxRep($map, $prefix . '.operations[' . $i . ']');
        }
        $ext = XdrTransactionV0Ext::fromTxRep($map, $prefix . '.ext');
        return new static($sourceAccountEd25519, $fee, $seqNum, $memo, $operations, $ext, $timeBounds);
    }
}
