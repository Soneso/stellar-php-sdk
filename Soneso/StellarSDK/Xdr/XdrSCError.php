<?php declare(strict_types=1);

// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

namespace Soneso\StellarSDK\Xdr;

class XdrSCError {

    public XdrSCErrorType $type;
    public ?int $contractCode = null;
    public ?XdrSCErrorCode $code = null;

    public function __construct(?XdrSCErrorType $type = null) {
        if ($type !== null) {
            $this->type = $type;
        }
    }

    public function encode(): string {
        $bytes = $this->type->encode();
        switch ($this->type->getValue()) {
            case XdrSCErrorType::SCE_CONTRACT:
                $bytes .= XdrEncoder::unsignedInteger32($this->contractCode);
                break;
            case XdrSCErrorType::SCE_WASM_VM:
            case XdrSCErrorType::SCE_CONTEXT:
            case XdrSCErrorType::SCE_STORAGE:
            case XdrSCErrorType::SCE_OBJECT:
            case XdrSCErrorType::SCE_CRYPTO:
            case XdrSCErrorType::SCE_EVENTS:
            case XdrSCErrorType::SCE_BUDGET:
            case XdrSCErrorType::SCE_VALUE:
            case XdrSCErrorType::SCE_AUTH:
                $bytes .= $this->code->encode();
                break;
            default:
                break;
        }
        return $bytes;
    }

    public static function decode(XdrBuffer $xdr): XdrSCError {
        $result = new XdrSCError(XdrSCErrorType::decode($xdr));
        switch ($result->type->getValue()) {
            case XdrSCErrorType::SCE_CONTRACT:
                $result->contractCode = $xdr->readUnsignedInteger32();
                break;
            case XdrSCErrorType::SCE_WASM_VM:
            case XdrSCErrorType::SCE_CONTEXT:
            case XdrSCErrorType::SCE_STORAGE:
            case XdrSCErrorType::SCE_OBJECT:
            case XdrSCErrorType::SCE_CRYPTO:
            case XdrSCErrorType::SCE_EVENTS:
            case XdrSCErrorType::SCE_BUDGET:
            case XdrSCErrorType::SCE_VALUE:
            case XdrSCErrorType::SCE_AUTH:
                $result->code = XdrSCErrorCode::decode($xdr);
                break;
            default:
                break;
        }
        return $result;
    }

    public function getType(): XdrSCErrorType { return $this->type; }
    public function setType(XdrSCErrorType $type): void { $this->type = $type; }
    public function getContractCode(): ?int { return $this->contractCode; }
    public function setContractCode(?int $contractCode): void { $this->contractCode = $contractCode; }
    public function getCode(): ?XdrSCErrorCode { return $this->code; }
    public function setCode(?XdrSCErrorCode $code): void { $this->code = $code; }

    public function toBase64Xdr(): string {
        return base64_encode($this->encode());
    }

    public static function fromBase64Xdr(string $xdr): static {
        $decoded = base64_decode($xdr, true);
        if ($decoded === false) {
            throw new \InvalidArgumentException('Invalid base64-encoded XDR');
        }
        return static::decode(new XdrBuffer($decoded));
    }

    public function toTxRep(string $prefix, array &$lines): void {
        $this->type->toTxRep($prefix . '.type', $lines);
        switch ($this->type->getValue()) {
            case XdrSCErrorType::SCE_CONTRACT:
                $lines[$prefix . '.contractCode'] = (string)$this->contractCode;
                break;
            case XdrSCErrorType::SCE_WASM_VM:
            case XdrSCErrorType::SCE_CONTEXT:
            case XdrSCErrorType::SCE_STORAGE:
            case XdrSCErrorType::SCE_OBJECT:
            case XdrSCErrorType::SCE_CRYPTO:
            case XdrSCErrorType::SCE_EVENTS:
            case XdrSCErrorType::SCE_BUDGET:
            case XdrSCErrorType::SCE_VALUE:
            case XdrSCErrorType::SCE_AUTH:
                $this->code->toTxRep($prefix . '.code', $lines);
                break;
            default:
                break;
        }
    }

    public static function fromTxRep(array $map, string $prefix): XdrSCError {
        $disc = XdrSCErrorType::fromTxRep($map, $prefix . '.type');
        $result = new XdrSCError($disc);
        switch ($result->type->getValue()) {
            case XdrSCErrorType::SCE_CONTRACT:
                $result->contractCode = TxRepHelper::parseInt(TxRepHelper::getValue($map, $prefix . '.contractCode') ?? '0');
                break;
            case XdrSCErrorType::SCE_WASM_VM:
            case XdrSCErrorType::SCE_CONTEXT:
            case XdrSCErrorType::SCE_STORAGE:
            case XdrSCErrorType::SCE_OBJECT:
            case XdrSCErrorType::SCE_CRYPTO:
            case XdrSCErrorType::SCE_EVENTS:
            case XdrSCErrorType::SCE_BUDGET:
            case XdrSCErrorType::SCE_VALUE:
            case XdrSCErrorType::SCE_AUTH:
                $result->code = XdrSCErrorCode::fromTxRep($map, $prefix . '.code');
                break;
            default:
                break;
        }
        return $result;
    }
}
