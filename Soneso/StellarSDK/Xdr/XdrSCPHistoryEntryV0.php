<?php declare(strict_types=1);

// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

namespace Soneso\StellarSDK\Xdr;

class XdrSCPHistoryEntryV0 {

    public array $quorumSets;
    public XdrLedgerSCPMessages $ledgerMessages;

    public function __construct(array $quorumSets, XdrLedgerSCPMessages $ledgerMessages) {
        $this->quorumSets = $quorumSets;
        $this->ledgerMessages = $ledgerMessages;
    }

    public function encode(): string {
        $quorumSetsCount = count($this->quorumSets);
        $bytes = XdrEncoder::integer32($quorumSetsCount);
        foreach ($this->quorumSets as $quorumSetsItem) {
            $bytes .= $quorumSetsItem->encode();
        }
        $bytes .= $this->ledgerMessages->encode();
        return $bytes;
    }

    public static function decode(XdrBuffer $xdr): XdrSCPHistoryEntryV0 {
        $quorumSets = [];
        $quorumSetsSize = $xdr->readInteger32();
        for ($i = 0; $i < $quorumSetsSize; $i++) {
            $quorumSets[] = XdrSCPQuorumSet::decode($xdr);
        }
        $ledgerMessages = XdrLedgerSCPMessages::decode($xdr);
        return new XdrSCPHistoryEntryV0($quorumSets, $ledgerMessages);
    }

    public function getQuorumSets(): array { return $this->quorumSets; }
    public function setQuorumSets(array $quorumSets): void { $this->quorumSets = $quorumSets; }
    public function getLedgerMessages(): XdrLedgerSCPMessages { return $this->ledgerMessages; }
    public function setLedgerMessages(XdrLedgerSCPMessages $ledgerMessages): void { $this->ledgerMessages = $ledgerMessages; }

    public function toBase64Xdr(): string {
        return base64_encode($this->encode());
    }

    public static function fromBase64Xdr(string $xdr): static {
        $decoded = base64_decode($xdr, true);
        if ($decoded === false) {
            throw new \InvalidArgumentException('Invalid base64-encoded XDR');
        }
        return static::decode(new XdrBuffer($decoded));
    }
}
